# Workjet correctness and clarity pass

You are editing an existing macOS SwiftUI menu-bar app and its installed worker wrappers. Implement the following user-requested corrections as one coherent pass. Do not redesign unrelated surfaces.

## Hard file whitelist

You may change ONLY:

- `app/Sources/WorkjetCore/Models.swift`
- `app/Sources/WorkjetCore/Drafts.swift`
- `app/Sources/WorkjetCore/ManagedPrompt.swift`
- `app/Sources/WorkjetCore/ViewModel.swift`
- `app/Sources/WorkjetCore/Service.swift`
- `app/Sources/WorkjetCore/Defaults.swift`
- `app/Sources/WorkjetCore/Telemetry.swift`
- `app/Sources/WorkjetCore/CLIProxy.swift`
- `app/Sources/WorkjetCore/RemotePiBootstrap.swift`
- `app/Sources/WorkjetCore/TailscaleDevices.swift` (new, if useful)
- `app/Sources/WorkjetApp/SettingsView.swift`
- `app/Sources/WorkjetApp/WorkerEditorView.swift`
- `app/Sources/WorkjetApp/ComputerEditorView.swift`
- `app/Sources/WorkjetApp/ActiveAreaView.swift`
- `app/Tests/WorkjetCoreTests/WorkjetCoreTests.swift`
- `bin/claude-sol`
- `bin/claude-kimi`
- `bin/claude-minimax`
- `bin/workjet-observe` (new, if useful)
- `install.sh`
- `tests/wrapper_rework_test.zsh`
- `README.md` only where behavior must be documented

Everything else is forbidden. If more scope is needed: STOP and justify it in the report; never widen scope on your own. No subagents.

## Product truth

Claude Code/Fable remains the only orchestrator. Workjet is only the `/workjet` skill/custom system prompt plus worker/model/harness/provider/computer configuration and truthful telemetry. Do not add workflow building or app-side worker selection.

The two user-facing harness names are exactly `Claude Code` and `Pi Code`. `Pi Sidecar`, daemon, transport, and pinned package version are internal implementation details and may appear only under technical details or in precise generated invocation/status text, never as the normal harness label. Preserve backwards-compatible internal enum decoding if needed, but change the visible/raw product naming safely.

## 1. One source of truth for worker prompts

- `Worker.instructions` is the canonical editable role/model-specific prompt. Preserve Markdown line breaks in the generated prompt; do not collapse it to one line.
- The generated Workjet prompt contains each worker's full instructions exactly once in a clearly delimited managed/read-only block.
- The settings UI shows one Systemprompt surface with:
  - editable general Fable/orchestrator rules (`skillRules` may remain the backwards-compatible storage field), clearly labeled as general rules only;
  - directly below, the generated worker portion, visibly present and text-selectable but not editable, clearly labeled "Automatisch aus Workern · nicht editierbar".
- Remove the user-facing toggle that disables generated worker declarations. Normalize legacy configs to worker injection enabled so worker blocks are always generated. Keep backwards decoding compatibility; do not destroy existing general rules.
- Worker editor labels the instructions field plainly (for example "Rolle und Anweisungen") and explains in one short sentence that it is copied into the system prompt.
- Give each worker a deterministic mention tag derived from its configured worker name: e.g. `Kimi-K3` -> `@Kimi-K3`, `UI/UX-Experte` -> a stable safe tag. The generated heading includes it. In the worker editor, show the other configured worker tags as small textual insertion buttons; clicking inserts the tag at the current practical end of the instructions. Detect unresolved `@...` references and show a concise warning. Avoid a new stored alias unless unavoidable.
- Marker injection must remain safe; generated prompt hash and handwritten prefix/suffix guarantees must remain intact.

## 2. Complete model and reasoning configuration

- Keep a free-form model text field.
- Replace the incomplete suggestion list with at least: `gpt-5.6-sol`, `gpt-5.6-terra`, `gpt-5.6-luna`, `claude-sonnet-5`, `claude-opus-5`, `k3[1m]`, `MiniMax-M3`. User-facing labels can be friendly but stored values must be explicit.
- Add an optional/backwards-compatible per-worker reasoning effort with direct compact choices. Include at least `low`, `medium`, `high`, `xhigh`, `max`, `ultra` plus an automatic/default state.
- Persist it, round-trip it through `WorkerDraft`, show it in the worker editor, and render it in the generated managed prompt. Existing config JSON must decode without it.
- Do not claim a runtime override that is not actually wired. If direct Claude CLI `--effort` is supported by the stored invocation, make the generated invocation truth explain how configured reasoning is applied; otherwise state that Fable must pass the configured effort. Do not silently mutate arbitrary custom executable arguments.

## 3. Truthful telemetry

- Fix stale runs caused by PID reuse. A run started 2026-07-29 with PID 691 must not be shown running when PID 691 now belongs to a process started 2026-07-31. Compare the probed process start token/time to run `started-at` with a small clock tolerance. Invalid/mismatched process identity yields interrupted, never running. Add a regression test.
- Direct configured wrappers (`claude-sol`, `claude-kimi`, `claude-minimax`) currently `exec claude` and therefore create no run record. Add minimal synchronous observation shared by those wrappers so future direct invocations create the existing `~/.local/state/workjet/runs` + `run-index` contract, worker name, started-at, exact child PID, heartbeat while alive, and terminal `exit` marker, while preserving stdin/stdout/stderr and exit code. Avoid logging prompt contents or secrets. It must work for foreground/headless calls, not detach them.
- Ensure existing `claude-agent` journal behavior is not double-counted: the observer must be bypassable via an environment flag set/documented for dispatcher-owned invocations, or detect an existing dispatcher context safely. Patch `claude-agent` is NOT in whitelist, so prefer a flag already present or wrapper logic that sees the dispatcher run environment; if impossible, STOP and report instead of editing outside scope.
- The Active UI should not show cryptic run IDs and heartbeat metadata as the primary third line. Show worker, elapsed time, and concise current activity/state. Put technical run ID/heartbeat in help/accessibility or omit from visible normal mode.

## 4. Real Tailscale device selection and simpler computer setup

- For Tailscale transport, discover devices from the allowlisted local Tailscale executable with `status --json`. Use the existing absolute executable allowlist. Never invoke a shell. Parse only required fields and cap stdout/time.
- Represent peers with stable ID/hostname, DNS name (trim trailing dot), first IPv4 if present, online state, and OS. Exclude the local `Self` from selectable remote peers.
- In ComputerEditor, Tailscale users choose a device directly from a compact list. Online devices first; offline state is visible. Selecting a device fills host and default name. Keep a small explicit "Host manuell eingeben" fallback, not the primary form. Provide refresh and honest errors when Tailscale is unavailable/not connected.
- Remove cryptic default-surface noise. Normal flow should be: connection, choose device (or SSH name/host), SSH user, simple sandbox toggle, simple activity toggle, setup status/action. Port, known-hosts path, audited bundle path, content hash, long sandbox/relay implementation details belong under a textual `Technische Details` disclosure. No dropdown for Local/remote machine selection, no decorative icons/cards.
- SSH remains direct fields and strict known-hosts behavior. Do not weaken security or silently fall back from Tailscale to ordinary SSH.
- `Prüfen & einrichten` remains disabled until truly deployable, but the UI must plainly state what is missing instead of looking broken.

## 5. Providers and authentication must actually be configurable

The present normal UI exposes internal credential references, login executables, and arguments while leaving the globally configured CLIProxy unavailable as a selectable worker provider. Replace that broken mental model with a functional, plain provider flow.

- The normal add/edit provider flow offers these user-facing access types: `Direkte API`, `CLIProxyAPI`, and `CLIProxy (Rust)`. The two proxy types are local compatible gateways through which subscription/OAuth providers are exposed. Preserve backwards decoding for existing provider kinds; do not pretend Workjet itself owns upstream OAuth if the gateway owns it.
- A provider has a human name, base endpoint, discovered/configured model IDs, connection status, and optional secret stored in Keychain. Add backwards-compatible persisted model IDs if needed.
- Do not ask users to invent or type Keychain reference IDs. Generate a stable internal reference from the provider UUID, accept the secret in a normal secure field, store it in Keychain, and show only `Zugang gespeichert` / `Kein Zugang gespeichert`. Management credentials and executable/argument internals belong under `Technische Details` only.
- For both local gateway types, require an allowlisted loopback endpoint and verify `/v1/models`; parse its OpenAI-compatible `data[].id` result. `CLIProxy (Rust)` is a distinct selectable provider type but may use the same compatible probe contract. Never silently substitute one gateway for the other.
- For a direct API, allow HTTPS endpoints (and loopback HTTP for local development), attach the configured bearer secret, and verify a user-authorized `/v1/models` route with tight time/output bounds and no redirects. Reject credentials embedded in URLs. Do not send a secret anywhere until the user explicitly presses the connection test.
- `Verbindung prüfen` updates provider state and discovered models and persists them. Honest failures remain visible.
- A configured CLIProxy gateway must become a real `Provider` that workers can select. Remove the disconnected global-only editor from the normal surface or migrate/synchronize it without duplicate maintenance.
- In WorkerEditor, provider selection is plain, and selecting a provider offers that provider's discovered/configured models first while retaining free-form model entry. A missing/deleted provider remains visibly unavailable and is never silently replaced.
- The generated system prompt renders provider name/type/endpoint safely and never includes keys, Keychain references, query strings, URL userinfo, or management secrets.
- Authentication owned by a subscription gateway is described plainly: e.g. `OAuth/Abonnement wird im lokalen Gateway verwaltet`; do not expose a raw login executable as if it were the primary Workjet setup.

## Acceptance criteria

Run exactly:

```sh
cd app && swift test
cd app && swift build -c release
zsh tests/wrapper_rework_test.zsh
zsh tests/dispatcher_test.zsh
```

Add focused tests for prompt multiline/mention rendering, missing mention detection, reasoning Codable/draft/rendering, Tailscale JSON parsing/order/errors, and PID-reuse rejection. Tests must not call the user's real Tailscale daemon or real model providers.

Also add focused provider tests: backwards Codable, safe endpoint validation for direct API versus loopback gateways, bearer header only after explicit probe, compatible model discovery, no secret leakage into generated prompt, and worker model suggestions using provider model IDs where practical.

Commit only if all commands are green, using exact commit message:

`fix(app): generate worker prompts and repair setup telemetry`

## Structured report tail

End with exactly:

```text
CHANGED: <short list>
TESTS: <commands and results>
OPEN_CONCERNS: <none or concise list>
PRODUCED_PATHS: <changed paths>
```
