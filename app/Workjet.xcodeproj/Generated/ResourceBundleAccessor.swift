import Foundation

// SwiftPM synthesizes this property for WorkjetApp's resources. The Xcode UI
// test host compiles the same production sources, so its equivalent is main.
extension Bundle {
    static let module = Bundle.main
}
